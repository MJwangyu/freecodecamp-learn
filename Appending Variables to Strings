我们不仅可以创建出多行的字符串，还可以使用加等号(+=)运算符来追加变量到字符串上。
任务
设置变量 someAdjective 的值，并使用 += 运算符把它追加到变量 myStr 上。

code:
// 举例
var anAdjective = "awesome!";
var ourStr = "Free Code Camp is ";
ourStr += anAdjective;

// 请只修改这条注释以下的代码

var someAdjective=" my joy.";
var myStr = "Learning to code is ";
myStr +=someAdjective;
